mutation updateReview($review: ReviewInputType!) {
  updateReview(review: $review) {
    id
    author_id
    course_id
    semester_id
    difficulty
    workload
    rating
    body
    created
    updated
  }
}
